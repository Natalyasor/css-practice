{{#with footer}}
<div class="footer">
    <p class="footer_copyright">{{copyright}}</p>
    <div class="nav_pages">
        {{#each pages}}
        <a href="{{path}}" class="pages">{{name}}</a>
        {{/each}}
    </div>
</div>
{{/with}}